# Mobile app (planned)

Mobile client for normal users. Will call the PROMPTHEUS API (`apps/api`) to run scans and view reports.

**Planned stack:** TBD (e.g. React Native, Flutter).

**No implementation yet** — this folder reserves the slot and documents intent.
