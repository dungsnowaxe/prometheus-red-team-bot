# Dashboard app

Streamlit web UI: enter target URL, run attack, view results table.

**Run:**

```bash
streamlit run apps/dashboard/main.py
# or (backward-compat shim)
streamlit run promptheus/interfaces/dashboard.py
```

Requires: `promptheus[dashboard]` (or `promptheus[all]`).
