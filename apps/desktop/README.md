# Desktop app (planned)

Native desktop client for normal users. Will call the PROMPTHEUS API (`apps/api`) to run scans and view reports.

**Planned stack:** TBD (e.g. Tauri + web frontend, or Electron).

**No implementation yet** — this folder reserves the slot and documents intent.
